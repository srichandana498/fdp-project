import { initializeApp } from "firebase/app";
import { getFirestore } from "firebase/firestore"; // if using database

// Import the functions you need from the SDKs you need
import { initializeApp } from "firebase/app";
import { getAnalytics } from "firebase/analytics";
// TODO: Add SDKs for Firebase products that you want to use
// https://firebase.google.com/docs/web/setup#available-libraries

// Your web app's Firebase configuration
// For Firebase JS SDK v7.20.0 and later, measurementId is optional
const firebaseConfig = {
  apiKey: "AIzaSyAAxyqBxpGgRxS1AgUSCiCfRTunSwXUgaI",
  authDomain: "fdp-2f772.firebaseapp.com",
  projectId: "fdp-2f772",
  storageBucket: "fdp-2f772.firebasestorage.app",
  messagingSenderId: "553604647459",
  appId: "1:553604647459:web:3f946af25d8db4f55abf96",
  measurementId: "G-L0BRBPY1PD"
};

// Initialize Firebase
const app = initializeApp(firebaseConfig);
const analytics = getAnalytics(app);

// Initialize Firebase
const app = initializeApp(firebaseConfig);

// Example: Firestore database
export const db = getFirestore(app);